const INITIAL_STATE = {
  source: [],
  loading: true
}

const elementReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    // case value:

    //     break;

    default:
      return state;
  }
}

export default elementReducer;